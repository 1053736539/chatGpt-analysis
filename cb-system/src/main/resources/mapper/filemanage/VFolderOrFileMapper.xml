<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cb.filemanage.mapper.VFolderOrFileMapper">
    <resultMap type="VFolderOrFile" id="VFolderOrFileResult">
        <result property="id"    column="id"    />
        <result property="parentId"    column="parent_id"    />
        <result property="name"    column="name"    />
        <result property="type"    column="type"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="fileUrl"    column="file_url"    />
        <result property="size"    column="size"    />
        <result property="extName"    column="ext_name"    />
        <result property="folderNum"    column="folder_num"    />
        <result property="fileNum"    column="file_num"    />
        <result property="secret"    column="secret"    />
    </resultMap>
    <sql id="selectVFolderOrFileVo">
        select id, parent_id, name, type,create_by ,create_time,file_url,size,ext_name,folder_num,file_num,secret from v_folder_or_file
    </sql>

    <select id="selectVFolderOrFileList" parameterType="VFolderOrFile" resultMap="VFolderOrFileResult">
        <include refid="selectVFolderOrFileVo"/>
        <where>  
            <if test="parentId != null  and parentId != ''"> and parent_id = #{parentId}</if>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="type != null "> and type = #{type}</if>
            and parent_id != '-1'
        </where>
        order by type, create_time desc
    </select>
    
    <select id="selectVFolderOrFileById" parameterType="String" resultMap="VFolderOrFileResult">
        <include refid="selectVFolderOrFileVo"/>
        where id = #{id}
    </select>

    <select id="selectVFolderOrFileByName" parameterType="VFolderOrFile" resultMap="VFolderOrFileResult">
        <include refid="selectVFolderOrFileVo"/>
        <where>
            <if test="name != null  and name != ''"> and name = #{name}</if>
            and parent_id ='0'
        </where>
    </select>

    <select id="selectVFolderOrFileListByParentId" parameterType="VFolderOrFile" resultMap="VFolderOrFileResult">
        <include refid="selectVFolderOrFileVo"/>
        <where>
            <if test="parentId != null  and parentId != ''"> and parent_id = #{parentId}</if>
        </where>
        order by type, create_time desc
    </select>

</mapper>