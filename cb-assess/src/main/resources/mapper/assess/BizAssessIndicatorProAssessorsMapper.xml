<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cb.assess.mapper.BizAssessIndicatorProAssessorsMapper">
    <resultMap type="BizAssessIndicatorProAssessors" id="BizAssessIndicatorProAssessorsResult">
        <result property="proId" column="pro_id"/>
        <result property="userId" column="user_id"/>
    </resultMap>
    <insert id="batchInsertProAssessors">
        insert into biz_assess_indicator_pro_assessors( pro_id,user_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{item.proId}, #{item.userId})
        </foreach>
    </insert>
    <delete id="deleteProAssessorsByProId" parameterType="String">
        delete
        from biz_assess_indicator_pro_assessors
        where pro_id = #{proId}
    </delete>

    <delete id="deleteProAssessorsByProIds" parameterType="String">
        delete from biz_assess_indicator_pro_assessors where pro_id in
        <foreach item="proId" collection="array" open="(" separator="," close=")">
            #{proId}
        </foreach>
    </delete>

</mapper>